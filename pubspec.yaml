name: pedidos_front
description: "Frontend para Orders API"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Iconos de estilo iOS
  cupertino_icons: ^1.0.8

  # 🔹 HTTP client para conectar con la API REST
  http: ^1.2.2

  # 🔹 Animaciones suaves y transiciones
  flutter_animate: ^4.5.0

  # 🔹 Manejo de estados (opcional pero útil)
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/google_logo.png
